_schema-version: "3.2"
ID: flights
description: A Fiori application.
version: 0.0.1
modules:
- name: flights-dest-content
  type: com.sap.application.content
  requires:
  - name: flights-dest-srv
    parameters:
      content-target: true
  - name: flights-repo-host
    parameters:
      service-key:
        name: flights-repo-host-key
  - name: flights-uaa
    parameters:
      service-key:
        name: flights-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: flights_repo_host
          ServiceInstanceName: flights-html5-srv
          ServiceKeyName: flights-repo-host-key
          sap.cloud.service: flights
        - Authentication: OAuth2UserTokenExchange
          Name: flights_uaa
          ServiceInstanceName: flights-xsuaa-srv
          ServiceKeyName: flights-uaa-key
          sap.cloud.service: flights
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: flights-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: flights-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - flights.zip
      name: flights
      target-path: resources/
- name: flights
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: flights-dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: flights-dest-srv
    service-plan: lite
- name: flights-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: flights-xsuaa-srv
    service-plan: application
- name: flights-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: flights-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
